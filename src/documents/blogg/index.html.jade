---
title: Blogg
layout: blogg-main
pageIndex: 12
dontIndexInAnyCollection: true
backgroundImage: "http://i.bldr.se/qkTj4.jpeg"
backgroundDark: true

isPaged: true
pagedCollection: "blogg"

---
- var blogg = getCollection("blogg").toJSON().slice(document.page.startIdx, document.page.endIdx)

h1.main-title(style=shouldWeUseADarkBackground())= document.title

.row-fluid()

  article#post.post

    .page-content
      .left-part
        each doc in blogg
          section.post
            header
              a.post-image(href= doc.url, style="background-image:url(" + doc.backgroundImage + ")")
              .post-date= toDateString(doc.date, "short")
              if (showCategory)
                a.post-category(href= "/blogg/" + doc.category)= capitalizeFirstChar(doc.category) + "/ "
              a.post-title(href= doc.url)= doc.title

            article.post-content
              p!= doc.intro

    .pagination
      ul
        - for (var pageNumber = 0; pageNumber < document.page.count; pageNumber++)
          - var num = pageNumber +1
          if document.page.number === pageNumber
            li.active
              span= num
          else
            li
              span
                a(href=getPageUrl(pageNumber))= num

        if hasNextPage()
          li
            span
              a(href=getNextPage()) Nästa
        else
          li.disabled
            span Nästa
