mixin pagination(page)
  ul
    - for (var pageNumber = 0; pageNumber < page.count; pageNumber++)
      - var num = pageNumber +1
      if document.page.number === pageNumber
        li.active
          span= num
      else
        li
          span
            a(href=getPageUrl(pageNumber))= num
    if hasNextPage()
      li
        span
          a(href=getNextPage()) Tidigare
    else
      li.disabled
        span Tidigare