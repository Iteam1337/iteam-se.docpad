---
layout: default
standalone: true
---

h1(title,class=document.background)= document.title

.row
  #page.page
    article.page-content

      if (document.pageType === 'Service')
        .col-md-6!= content
        .col-md-6
          h4!= document.tagHeader
          include ../jade_includes/service

          if document.certifications
            each img in document.certifications
              img.certification(src=img)
      else
        .col-md-12!= content

include ../jade_includes/call-to-action
