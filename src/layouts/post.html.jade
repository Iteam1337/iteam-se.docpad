---
layout: default
---

h1.main-title(title,class=shouldWeUseADarkBackground())= document.title

.row-fluid(make-pretty)
  article#post.post
    .post-date= document.date.toDateString()
    h2.post-title= document.title
    .post-content!= content
    .post-author
      if document.author
        include ../mixins_and_partials/jade_includes/author
